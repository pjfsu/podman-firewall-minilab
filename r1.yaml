apiVersion: v1
kind: Pod
metadata:
  name: r1
spec:
  containers:
    - name: r1
      image: docker.io/openwrt/rootfs:latest
      securityContext:
        runAsUser: 0
        capabilities:
          add: ["NET_ADMIN", "NET_RAW"]
      command: ["/sbin/init"]
